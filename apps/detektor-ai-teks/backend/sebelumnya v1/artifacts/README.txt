Letakkan file model hasil pelatihan di folder ini:

    model_detektor_catboost_469.joblib

File itu dihasilkan oleh notebook_export/export_model.py yang dijalankan di
lingkungan training (Colab/lokal tempat Xdf, y, build_preprocessor, make_models
tersedia). Tanpa file ini, backend akan menolak start dengan pesan yang jelas.
